# Variable names only. Never add real secret values or machine-specific paths.
# Batch 2 connector secret references. Values must be supplied only in the host environment.
GSC_ACCESS_TOKEN=
GA4_ACCESS_TOKEN=
AHREFS_API_KEY=
PAGESPEED_API_KEY=
CRUX_API_KEY=

# Contract-only future integrations. No connector uses these in Batch 2.
BING_WEBMASTER_API_KEY=
SHOPIFY_ACCESS_TOKEN=

# Runtime locations may be configured outside Git when the execution layer is activated.
SEO_OS_AUTHORIZATION_MANIFEST=
SEO_OS_DATA_ROOT=
